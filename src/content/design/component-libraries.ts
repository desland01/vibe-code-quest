import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("component-libraries", "Component libraries", "A component library packages repeated interface behavior and appearance into maintained building blocks.");
