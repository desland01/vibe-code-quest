# L-003 — Competence XP

**UTC close:** 2026-07-21
**Result:** PASS
**Base:** L-002 `4f1616f`

## Formula (locked)

| Award key | Points | Trigger |
|---|---:|---|
| `scenario_solved` | 15 | `furthestBeatIndex` past scenario beat |
| `gotcha_solved` | 15 | `furthestBeatIndex` past gotcha beat |
| `check_passed` | 20 | `checked === true` |
| `landmark_stamped` | 50 | `completed === true` |
| **Max / landmark** | **100** | 15+15+20+50 |

- Zero penalties, zero decay
- Predict never awards (any pick advances; correctness is not a server fact)
- Awards derived from **server-merged** progress RETURNING state only
- Unique key `(profile_id, region, landmark, award_key)` → idempotent
- Backfill: all-time XP yes; `awarded_at` clamped before current UTC week

## Disposable Neon branch proof

See [`L-003-branch-proof.md`](./L-003-branch-proof.md).

- Branch: `vibe-launch-l003-2026-07-21T20-51-01-079Z` / `br-summer-math-at3m4811`
- Parent: `main` / `br-raspy-bread-atcew3is`
- Migrations: applied `0009_xp.sql`
- `xp.integration.test.ts` 6/6 PASS
- `beatProgress.integration.test.ts` 3/3 PASS (incl. concurrent race)
- Branch deleted and verified absent from branches list
- No secrets printed

## Main Neon migrate

Precheck (unpooled endpoint matched main connection-string base `ep-wandering-mouse-atbkiw6k`):

```json
{
  "xp_awards": null,
  "migrated_0009": false,
  "mig_count": 8
}
```

Applied via `npm run db:migrate` (uses `DATABASE_URL_UNPOOLED`).

Postcheck (pooled + unpooled agree):

```json
{
  "xp_awards": "xp_awards",
  "migrated_0009": true,
  "mig_count": 9,
  "privileges": {
    "can_select": true,
    "can_insert": true,
    "can_update": false,
    "can_delete": false
  },
  "award_rows": 438,
  "weekly_window_rows": 0
}
```

`rls-test` branch left untouched. Disposable branch still absent after migrate.

## Implementation

| Path | Role |
|---|---|
| `db/migrations/0009_xp.sql` | Additive `xp_awards` + RLS + backfill |
| `src/server/xp.ts` | Pure formula + idempotent apply |
| `app/api/progress/route.ts` | Atomic progress upsert + XP in one transaction; GET returns `{ items, xp: { total } }` |
| `src/components/XpHud.tsx` | Map HUD counter (server total only; quiet hide on failure) |
| `src/components/MapExperience.tsx` | Mounts HUD in header actions |
| `src/components/landmark/Beats/BeatPlayer.tsx` | Emits `xp_awarded` only when `newPoints > 0` |
| Analytics 4-file seam | `xp_awarded` added to union + client allowlist + unit contract |

## Gates

| Gate | Result |
|---|---|
| `npm run typecheck` | ✅ clean |
| `npm run lint` | ✅ 0 errors / 1 pre-existing OnboardingChat warning |
| `npm run test` | ✅ 159 passed / 28 skipped (integration needs `TEST_DATABASE_URL`; live proof was separate on disposable branch) |
| `npm run build` | ✅ 48 sequences |

## Playwright (`http://localhost:3100` only)

- Fresh `npm run dev -- -p 3100`
- `e2e/xp.spec.ts` 2/2 PASS
- Full suite **workers=1: 46/46 PASS**
- Parallel default workers twice hit 45/46: `onboarding.spec.ts` hung with controls disabled waiting for `/Map unlocked/` (same parallel-load flake class as KICKOFF §6). Solo + workers=1 full suite both PASS. **Did not timeout-bump.**

## Visual / layout proof

**Vision-analyze provider was credit-blocked (OpenRouter 402).** No alternate vision path; no spend. Substitute evidence only:

1. Rendered screenshots under `evidence/L-003/` (owner eyeballs; **not** claimed as subjective visual PASS).
2. Mechanical browser geometry + computed-style inspection at desktop 1280 and mobile 390 (PASS):
   - HUD visible with `100XP` / `aria-label="100 experience points"`
   - min-height 44px, hard 2px ink border, banner bg, 3px hard offset shadow
   - no glass blur / neon glow
   - no page horizontal overflow
   - HUD fully inside viewport
   - no overlap with Upgrade button

### Screenshot inventory

| File | Notes |
|---|---|
| `xp-hud-desktop.png` | Canonical desktop capture from e2e |
| `xp-hud-mobile.png` | Canonical mobile capture from e2e |
| `inspect-desktop.png` | Geometry-probe capture (same state) |
| `inspect-mobile.png` | Geometry-probe capture (same state) |

## Secret hygiene

- Connection strings never printed
- No credentials in evidence
- Accidental one-shot `npx vitest` during verification (R010 slip) — immediately corrected; all canonical re-runs used `bunx`/`npm`. No package.json or lockfile churn.

## Constance / foreign dirt left uncommitted

- `CLAUDE.md`, `constants.md`, `constants.md.reground-log.jsonl`
- `.claude/*`, `.agents/*`
- `public/content-manifest.v1.json` timestamp churn restored

## Next

L-004 Leaderboard.
