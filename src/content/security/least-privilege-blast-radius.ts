import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("least-privilege-blast-radius", "Least privilege and blast radius", "Limit every user, service, and agent to only the authority and resources its job requires.");
