import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("dependencies-and-work-graphs", "Dependencies and work graphs", "A work graph makes prerequisites, parallel-safe tasks, and blockers explicit.");
