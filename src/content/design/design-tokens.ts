import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("design-tokens", "Design tokens", "Tokens name recurring visual decisions such as color roles, spacing steps, and radii.");
