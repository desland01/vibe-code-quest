import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("working-tree-hygiene", "Working-tree hygiene", "Working-tree state distinguishes tracked, untracked, generated, ignored, and modified project files.");
