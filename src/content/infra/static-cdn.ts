import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("static-cdn", "Static + CDN", "Static hosting and a content delivery network suit sites that do not need a request-time backend.");
