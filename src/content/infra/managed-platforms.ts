import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("managed-platforms", "Managed platforms", "Managed application platforms run services while taking on much of the infrastructure operations burden.");
