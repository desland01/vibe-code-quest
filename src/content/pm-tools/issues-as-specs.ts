import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("issues-as-specs", "Issues as executable specs", "A strong issue states an outcome, boundaries, acceptance checks, and the context needed to verify completion.");
