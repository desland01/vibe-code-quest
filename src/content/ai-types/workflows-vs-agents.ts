import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("workflows-vs-agents", "Workflows vs agents", "Workflows follow code-defined steps, while agents dynamically choose what to do next.");
