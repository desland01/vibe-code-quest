import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("pull-requests-and-review", "Pull requests and review", "A pull request presents a proposed change, its evidence, and the review conversation around it.");
