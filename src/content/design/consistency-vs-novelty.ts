import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("consistency-vs-novelty", "Consistency vs novelty", "Routine interactions should remain predictable while a few meaningful moments can express product character.");
