import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("revert-and-recovery", "Revert and recovery", "Version-control recovery can back out bad changes or locate earlier working states without erasing history.");
