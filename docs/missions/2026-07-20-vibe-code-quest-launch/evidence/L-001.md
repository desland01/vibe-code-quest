# L-001 — Live SQL concurrency proof

**UTC start:** 2026-07-21T08:53:19.591Z
**UTC close:** 2026-07-21T08:53:30.723Z
**Result:** PASS

## Preflight note

- Attempt 1: created `br-quiet-lab-atsu5m8c`; failed before connection-string parse (table-output scrape + `--pooled false` argv). Branch deleted and verified absent. No secrets printed.
- Attempt 2: fixed JSON parse + unpooled default; failed on multi-role ambiguity (`app_user` NOLOGIN + `neondb_owner`). Branch `br-red-rain-atie5grw` deleted and verified absent.
- Attempt 3: runner rewrite with `--role-name neondb_owner`, JSON connection-string parse, race test via eight concurrent `pool.query` upserts over max:2 pool (no client-checkout deadlock).

## Disposable Neon branch

- name: `vibe-launch-l001-2026-07-21T08-53-19-589Z`
- id: `br-dawn-firefly-atybpffe`
- parent: `main` (`br-raspy-bread-atcew3is`)
- role: `neondb_owner`
- migrations: exit 0 — already-present / no-op
- deleted: yes (verified absent from branches list)

## Test

- file: `src/__tests__/beatProgress.integration.test.ts`
- suite: beat progress SQL merge (guarded by `TEST_DATABASE_URL`)
- scenarios:
  1. sequential insert / GREATEST / stale absorb / checked OR / terminal stamp / no-regress
  2. legacy non-beat state wholesale replace on first beat write
  3. **concurrent race** — 8 parallel `pool.query` upserts (mid checked, terminal stamp, stale lower indexes) over a 2-connection pool; final row must be furthestBeatIndex=7, checked=true, completed=true, stampedAt retained

## Result

- vitest exit: 0
- test files passed: 1
- tests passed: 3
- failed: 0
- skipped: 0
- failure_reason: none

## Secret hygiene

- connection string captured in process memory only
- not printed, not written to evidence, not committed
- `TEST_DATABASE_URL` unset after process exit

## Constance

- destructive branch delete checked before execution (ownership_verified=true, created_this_run=true, external_spend_usd=0)
