import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("branches-as-isolation", "Branches as isolation", "A branch isolates a line of work until it is ready to combine with other changes.");
