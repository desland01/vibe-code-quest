import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("decision-logs", "Decision logs", "A decision log preserves what was chosen, why, and what would trigger reconsideration.");
