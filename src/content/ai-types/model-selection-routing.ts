import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("model-selection-routing", "Model selection and routing", "Model routing chooses among capability, speed, cost, and modality for each request.");
