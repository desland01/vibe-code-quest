import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("authentication-vs-authorization", "Authentication vs authorization", "Authentication identifies an actor; authorization determines which actions and records that identity may access.");
