import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("trust-boundaries", "Trust boundaries", "A trust boundary marks where data or control crosses between differently trusted systems.");
