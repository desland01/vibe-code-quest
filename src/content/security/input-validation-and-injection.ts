import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("input-validation-and-injection", "Input validation and injection", "Untrusted input must be validated and passed through safe APIs at every boundary.");
