# Engineering Review — code-tutor v1 packet

Reviewer: Codex (GPT-5.6-sol, ChatGPT-plan subscription, read-only sandbox), fresh context, did not write the packet. Full transcript: `/tmp/codex-eng-review.txt` (session 019f4e07-108d-7d92-92ae-35eb68e85750, 2026-07-10). Verdict as delivered: **BLOCKING — 12 blocking, 9 non-blocking.**

Independence: genuine (separate vendor, separate process, no shared context).

## Findings and dispositions

Every finding retained per audit-trail rule. "Resolved in" points at the packet v2 revision (PRD/VALIDATION_CONTRACT rewritten 2026-07-10 after this review).

| # | Finding | Sev | Disposition |
|---|---|---|---|
| 1 | Anonymous identity untrustworthy for billing/cost caps (storage-clear = new identity; disposable accounts drain budget) | Critical | **accepted** — v2: verified email REQUIRED to start trial; anonymous users get a small metered guide allowance behind IP/device throttles + global hard cap; per-user caps bind to verified accounts; VAL-035 extended with multi-anon aggregate-throttle test. |
| 2 | Trial semantics unresolved ("with card OR without card") | Critical | **accepted** — v2 decides: **no-card trial, email verification required to start it**; trial expiry → paywall (no auto-charge possible); card entered only at explicit subscribe. Exact lifecycle table added to PRD REQ-013; VAL-040 rewritten with exact assertions. |
| 3 | Webhook entitlement has no consistency model | Critical | **accepted** — v2 REQ-013 specifies entitlement table (customer/subscription IDs, status, trial_end, period_end, event id + created timestamp), unique processed-event IDs, idempotent transactional handlers, out-of-order precedence by event.created, post-checkout server-side reconciliation, scheduled reconciliation; VAL-040 tests dup/reversed/retry/unknown-customer/checkout-before-webhook. |
| 4 | Cost cap race-prone, no accounting definition | Critical | **accepted** — v2 REQ-012: atomic DB reservation before each attempt (worst-case tokens), post-call reconcile, one ledger covering executor+fallback+advisor, UTC day boundary, pricing table versioned in config, reservation expiry; VAL-035 tests parallel-near-limit, retry chains, abandoned streams, midnight rollover. |
| 5 | Anon→email migration underspecified (cross-device magic link, existing-account merge, replay) | High | **accepted** — v2 REQ-006: same-browser OTP (6-digit code, not magic link) to guarantee session continuity; existing-email collision → explicit "sign in instead, anonymous progress merges on conflict-free keys, newest-wins on conflict"; idempotent writes + uniqueness constraints; VAL-022 extended with failure/interrupt/replay/collision paths. |
| 6 | RLS model underspecified per-operation | High | **accepted** — v2 REQ-005 defines per-table × per-op policy matrix incl. WITH CHECK, server-side id constraints, `(profile_id, region, landmark)` uniqueness, telemetry payload validation + size limits; VAL-021 tests forged-owner insert/upsert/delete + service-role boundary. |
| 7 | Stripe customer not bound to Supabase identity | High | **accepted** — v2 REQ-013: Checkout sessions created server-side from verified JWT only; supabase user id in Stripe metadata AND server-owned mapping table with uniqueness; webhooks validate object relationships; test cross-user session attach. |
| 8 | Pixi a11y described as parallel UI | High | **accepted** — v2 REQ-003 reframed: **DOM interaction layer is canonical** (buttons/links drive the same reducer), Pixi/SVG is presentation only; focus order/restoration, zoom announcements, escape/back, touch targets specified; keyboard e2e + SR script + 200/400% zoom + WebGL-failure fallback in VAL-011. |
| 9 | Static content generation technically confused (`generateStaticParams` ≠ JSON emitter; two sources of truth) | High | **accepted** — v2 REQ-007: ONE pipeline — Zod schema + TS content modules → build script emits versioned JSON manifest → routes and runtime fallback both consume the manifest; build with network disabled must pass; invariants (8 regions × 6 landmarks, unique ids) asserted at build. |
| 10 | LLM behavior non-deterministic vs contract (turn cap, "low confidence", Sonnet-graded MCQ) | High | **accepted** — v2: onboarding = deterministic state machine w/ schema-validated structured output + server-side question counter; escalation = explicit rule list (recorded per decision); **quiz graded deterministically from canonical answers** (Sonnet only explains, never grades); prompt-injection/malformed/timeout tests added. |
| 11 | Test strategy misses real boundary (no e2e framework, no local Supabase migrations, no webhook signature tests, no CI) | High | **accepted** — v2 Testing Decisions: Playwright added as gate; `supabase start` local stack from committed migrations + seeded users; route-handler e2e; Stripe signature verification + fixture replay through real handler; Gateway mocked at HTTP transport (429/5xx/timeout/malformed/partial stream); GitHub-Actions-equivalent CI via Vercel checks documented. |
| 12 | Prod failure toggles via env vars unsafe | High | **accepted** — v2 VAL-061: failure drill via signed owner-only test header operating on a canary account; production build rejects persistent forced-failure config; ordinary users can never trigger/observe. |
| 13 | OG/share leaks identity or breaks for crawlers | High | **accepted** — v2 REQ-014: immutable share snapshot + unguessable token (not user id), user-approved fields only, revoke/rotate, server-safe fonts, cache semantics; crawler/unknown-token/unicode/empty-progress tests. |
| 14 | Perf/cost targets unmeasurable | High | **accepted** — v2 adds budgets: p75 LCP ≤ 2.5s (mid-tier mobile, Fast 3G→4G profile) / INP < 200ms / CLS < 0.1; initial route JS ≤ 300KB gz with Pixi/Stripe/AI lazy-loaded behind interaction; map assets ≤ 2MB total, textures ≤ 2048px; per-session guide budget 50K tokens; default caps per-user $0.50/day, global $10/day (config). Bundle analysis + browser traces in VAL-013. |
| 15 | URL/reducer ownership undefined | Medium | **accepted** — v2 REQ-004: URL is navigation source of truth (`/map`, `/map/<region>`, `/map/<region>/<landmark>?format=`); reducer projects navigation; deep-link/refresh/back-forward/invalid-id tests in VAL-012. |
| 16 | Content/visited-state versioning absent | Medium | **accepted** — v2 REQ-007: stable landmark ids + manifest schema version; progress keyed to stable ids; removed-landmark rule (progress row retained, rendered from last manifest containing it); share snapshots immutable by design. |
| 17 | Vercel WA treated as event database | Medium | **accepted** — v2 VAL-051 asserts instrumentation (unit test on event construction + browser test on dispatch); dashboard visibility demoted to manual operational evidence. (Also flagged by adversarial review F10.) |
| 18 | Serial-agent recovery artifacts are promises, not state | High | **accepted (phase-timing)** — ISSUES/EXECUTION_PLAN/HANDOFF are later phases by design (ADR-0002 reviews-before-issues); now written with issue ids, dependencies, owned files, preconditions, evidence paths, resume rules. mission-state issue_count updated when ISSUES.md landed. |
| 19 | Repo truth contradicts scope (`vibe-tutor` name, 2-deep test lock, `stub` status) | Medium | **accepted** — ISSUE-001 (first atomic slice) normalizes scaffold: rename, replace obsolete test with 8×6 invariants, drop `stub` status, runtime Zod validation. |
| 20 | Deployment authority contradiction (INTERVIEW vs MISSION_CONTEXT vs global gates) | High | **accepted** — v2 guardrail: preview deploys autonomous; production deploys of content-only milestones autonomous per D3; production deploy of billing/email surfaces additionally gated on legal pages existing (VAL-043) AND the D3 grant re-confirmed at the approval gate for the now-paid product (adversarial F9). MISSION_CONTEXT wording fixed. |
| 21 | Content currency has no maintenance mechanism | Medium | **accepted** — v2 REQ-008: each landmark carries `sources[]` (url + checked date); stale-content report script; durable-concept vs vendor-claim fields separated. |

## Verdict after revision

All 12 blocking findings addressed in packet v2 (PRD + VALIDATION_CONTRACT rewritten; issues/plan/handoff materialized). `reviews.eng = pass` contingent on ISSUES_VALIDATION confirming REQ/VAL mappings.
