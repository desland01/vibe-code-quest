# Vibe Tutor Work Ledger

## Completed Work

- 2026-07-24: Launch mission L-005 collectibles + stamped map glow PASS. No new Neon migration — ownership from existing `progress.state.completed === true` only. Browser-safe registry `src/lib/collectibles.ts` (48 unique id/name/sigil; glow marker upsert/consume/TTL; shame-term list). BeatPlayer: grant only after server-merged PUT confirms completed; fresh stamp gesture sets sessionStorage marker + same-tab event; resume static (no live status, no stampFresh). LandmarkView keys BeatPlayer by region/landmark. SubMapScene: region-overview shelf (Earned/Open), stamped card labels + one-shot glow (all unconsumed owned markers for region; cross-region preserved); region-keyed owned/fresh state; `onAnimationEnd` settles glow. CSS: grant tile, stampFresh, landmark-glow 1200ms ease-in-out ×3 → settled, reduced-motion none. Units `collectibles.test.ts` 6/6 (registry, strict completed, marker lifecycle + sibling preserve, shame). Playwright on `http://localhost:3100` only: collectibles e2e 2/2 (held terminal PUT no optimistic grant + failed PUT no grant + shelf/glow/resume/reduced-motion/mobile overflow); full suite **workers=1 50/50**. Gates: typecheck ✅ lint 0 errors/1 pre-existing OnboardingChat warning ✅ 179 unit / 39 skipped ✅ build 48 sequences ✅. Vision-analyze OpenRouter 402 — geometry + overflow + screenshots under `evidence/L-005/` substitute, not subjective visual PASS. Evidence: `evidence/L-005.md` + comps `comp-judgement.md` + shelf-glow desktop/mobile PNGs. Foreign Constance/`.claude`/`.agents`/constants/manifest left uncommitted.

- 2026-07-24: Launch mission L-005 comps gate PASS (docs-only; no production UI). HTML comps `designs/comps/vibe-launch-l005/collectible-glow-comps.html` (3 states: stamp+static collectible grant, collection shelf, region-map stamped glow) + Playwright `file://` renderer `docs/missions/2026-07-20-vibe-code-quest-launch/scripts/render-l005-comps.mjs`. Six captures desktop 1280×800 + mobile 390×844 (shelf/map mobile full-section taller than viewport — expected). Renderer exit 0: Press Start 2P loaded; no overflow; shadow budget inside viewport; interactive targets ≥44px; shelf/map grid 3-col desktop / 1-col mobile; no blur/glass; stamp motion 480ms steps(6)×1; glow motion 1200ms ease-in-out×3 then settled; reduced-motion stamp+glow animation none with labels still legible; shame-term scan clean; stamped/open not color-only. Judge record `evidence/L-005/comp-judgement.md`: mechanical/source PASS; vision-analyze OpenRouter 402 — not subjective visual PASS; owner may eyeball PNGs. Locked for impl: derive ownership from server `completed===true` (no new table preferred); collectible static inside stamp moment (no third celebration); glow only on SubMapScene DOM cards; animated glow once per just-confirmed stamp then settled; resume=settled only; positive Open/Earned copy. Foreign Constance/`.claude`/`.agents`/constants left uncommitted.

- 2026-07-24: Launch mission L-004 opt-in leaderboard PASS. Accidental one-shot `npx tsx …/cli/constance.ts` at session start (R010 slip; path missing, no package/lockfile churn) immediately corrected to `node /Users/thebeast/Constance/dist/constance.mjs`; all subsequent tooling used bunx/node. Additive `db/migrations/0010_leaderboard.sql` (`leaderboard_entries` + soft opt-out + partial unique active-handle index + RLS select/insert/update no delete; `leaderboard_write_limits` HMAC-only table with RLS and zero app_user table privs; SECURITY DEFINER `leaderboard_board` + `leaderboard_register_write`, PUBLIC execute revoked). Server surface in `src/server/leaderboard.ts` + browser-safe `src/lib/leaderboard.ts`: handle 3–24 sanitized, weekly/all-time boards from `xp_awards` only, top-N + own rank, positive-only copy (R041), 10s mutation cooldown under profile lock, HMAC IP/session write bucket max 20/hour (cap cannot be raised by callers), no-DB GET unavailable / PUT refuses hosted writes. Routes `GET|PUT|DELETE /api/leaderboard`, page `/leaderboard`, map “Quest board” link, cozy-pixel CSS. Fresh disposable Neon proof PASS overwrote STALE pre-limiter evidence: branch `vibe-launch-l004-2026-07-24T14-38-24-121Z` / `br-sweet-glitter-atu37lne` (parent main); mig_count 10; leaderboard.integration 11/11 + xp.integration 6/6; branch deleted+verified absent. Main Neon after endpoint-base match `ep-wandering-mouse-atbkiw6k`: precheck mig_count=9/entries=null → postcheck mig_count=10/entries+write_limits present on pooled+unpooled; `rls-test` untouched. Gates: typecheck ✅ lint 0 errors/1 pre-existing OnboardingChat warning ✅ 173 unit / 39 skipped ✅ build 48 sequences + `/api/leaderboard` + `/leaderboard` ✅. Playwright on `http://localhost:3100` only: leaderboard e2e 2/2 (cookie-free public GET; map link + opt-in/out + 429 rename before screenshots + weekly/all-time + leave); full suite **workers=1 48/48**. Vision-analyze OpenRouter 402 — geometry + overflow asserts + screenshots under `evidence/L-004/` substitute, not subjective visual PASS. Evidence: `evidence/L-004.md` + `evidence/L-004-branch-proof.md` + scripts `l004-branch-proof.mjs` / `l004-main-migrate.mjs`. Foreign Constance/`.claude`/`.agents`/constants/manifest left uncommitted.

- 2026-07-21: Launch mission L-003 competence XP PASS. Additive `db/migrations/0009_xp.sql` (`xp_awards` + RLS + backfill; app_user SELECT/INSERT only). Server formula in `src/server/xp.ts`: scenario 15 + gotcha 15 + check 20 + stamp 50 = 100 max/landmark; zero penalties/decay; predict never awards; awards from server-merged RETURNING state only; unique `(profile_id, region, landmark, award_key)`. Progress route one transaction: beat upsert + `applyXpAwards`; GET returns `{ items, xp: { total } }`. Map HUD `XpHud` (server total only; quiet hide on failure) + cozy-pixel CSS; BeatPlayer emits `xp_awarded` only when `newPoints > 0`; analytics 4-file seam extended. Disposable Neon proof PASS (`vibe-launch-l003-2026-07-21T20-51-01-079Z` / `br-summer-math-at3m4811`, parent main; xp.integration 6/6 + beatProgress.integration 3/3 incl. race; branch deleted+verified absent). Main Neon migrate after endpoint-base match: precheck mig_count=8/xp_awards=null → postcheck mig_count=9/xp_awards present/438 backfill rows/weekly_window=0 on pooled+unpooled. Gates: typecheck ✅ lint 0 errors/1 pre-existing OnboardingChat warning ✅ 159 unit / 28 skipped ✅ build 48 sequences ✅. Playwright on `http://localhost:3100` only: xp e2e 2/2; full suite **workers=1 46/46**. Parallel default workers twice hit 45/46 on `onboarding.spec.ts` (controls disabled waiting Map unlocked — newly observed parallel-load-only flake, not timeout-bumped; solo + workers=1 PASS). Vision-analyze OpenRouter 402 — geometry + computed-style + screenshots under `evidence/L-003/` substitute, not subjective visual PASS. user-rules R042–R047 declared. Accidental one-shot `npx vitest` during verification (R010 slip) immediately corrected; no package/lockfile churn. Evidence: `evidence/L-003.md` + `evidence/L-003-branch-proof.md`. Foreign Constance/`.claude`/`.agents`/manifest left uncommitted.

- 2026-07-21: Launch mission L-002 beat factory PASS. Registered all 48 landmarks: 2 hand-authored overrides (`git/commits-as-checkpoints`, `security/trust-boundaries`) + 46 deterministic same-landmark canonical projections via `src/content/beats/derive.ts` (8-beat grammar hook→predict→reveal→scenario→gotcha→default→check→recap; no sibling-landmark text; quiz reserved for check/anti-spoiler). Per-option feedback is source-aware: correct options use fixed positive leads; wrong options use a fixed source-class frame plus the exact canonical label (no invented facts); units lock `allowedWrongFeedbacks`, label inclusion, and per-beat wrong-feedback uniqueness. Other unit locks: field mapping, exact-composite provenance allowlist, determinism, correct-option slot rotation, quiz content absent from beats 0–5. Fallout: invalid `?format=` falls back to overview not Play (`app/map/[region]/[landmark]/page.tsx`); analytics pins `#landmark-title` (BeatPlayer also renders h3 title); landmark-formats expects Play tab (every landmark beat-enabled). JWT session tamper test stabilized (payload-segment flip — last signature char can be a no-op under base64url padding). Spot-check e2e one landmark/region + mobile sql (`e2e/factory-spotcheck.spec.ts`) with mechanical no-horizontal-overflow geometry at scenario+stamp; screenshots under `docs/missions/2026-07-20-vibe-code-quest-launch/evidence/L-002/` (vision-analyze OpenRouter 402 — geometry + screenshots substitute, not subjective vision PASS). Evidence: `evidence/L-002.md`. Full Playwright **44/44** on `http://localhost:3100` workers=1 (never 127.0.0.1 — Next hydration/HMR block). Gates: typecheck ✅ lint 0 errors/1 pre-existing OnboardingChat warning ✅ 149 unit / 22 skipped ✅ build 48 sequences ✅. Foreign Constance/`.claude`/`.agents`/manifest churn left uncommitted.

- 2026-07-21: Launch mission L-001 LIVE SQL concurrency proof PASS. Disposable Neon branch `vibe-launch-l001-2026-07-21T08-53-19-589Z` / `br-dawn-firefly-atybpffe` (parent main/`br-raspy-bread-atcew3is`, role `neondb_owner`). Migrations already present on branch (exit 0). `src/__tests__/beatProgress.integration.test.ts` 3/3 including concurrent race (eight concurrently submitted upserts over a two-connection pool — GREATEST index, checked/completed OR-latch, first stamp wins, stale absorbed). Branch deleted and verified absent from branches list. Two earlier attempts cleaned up with no secrets printed (`br-quiet-lab-atsu5m8c` connection-string parse; `br-red-rain-atie5grw` multi-role ambiguity). Evidence: `docs/missions/2026-07-20-vibe-code-quest-launch/evidence/L-001.md`. QA-MATRIX concurrent-write row flipped to LIVE SQL PASS. Owner amended A4.3 spelling to **Truline** / https://truline.io (never Trueline) — KICKOFF updated; Constance R039 `public_product_name`, R040 `truline_byline_url`, R041 `leaderboard_shame_copy` declared into user-rules.md; historical 2026-07-20 ledger line left intact (R018). Pre-commit gates: typecheck ✅ lint 0 errors/1 pre-existing OnboardingChat warning ✅ 146 unit / 22 skipped (3 beatProgress integration skipped without TEST_DATABASE_URL; live proof was separate) ✅ build 2 sequences ✅.

- 2026-07-20: engagement-v2 E-005 transfer + QA gate. Registered `security/trust-boundaries` beat sequence (copy-loyal to canonical landmark; same 8-type grammar as pilot; zero landmark-specific UI branches). Registry count 2 validated at build. Unit registry test locks both sequences. Parameterized Playwright transfer gate (`e2e/beats.spec.ts` E-005 describe) asserts type order + stamp-once + next-offer through the same BeatPlayer path; evidence under `docs/missions/2026-07-19-code-tutor-engagement-v2/evidence/E-005/`. Authored `QA-MATRIX.md` (VALIDATION_CONTRACT E-005 rows; concurrent-write live SQL still unit-only pending `TEST_DATABASE_URL` from E-001) and `USABILITY-PROTOCOL.md` (5–8 founder study; factory blocked until founder sign-off). Pre-existing `landmark-formats.spec.ts` load flake under parallel full-suite noted, not timeout-bumped. Foreign Constance/`.claude`/`.agents` dirt left untouched. Gates: typecheck ✅ lint 0 errors/1 pre-existing warning ✅ 146 unit ✅ build 2 sequences ✅ beats e2e 9/9 on :3100 ✅.

- 2026-07-20: engagement-v2 E-002 BeatPlayer + E-003 pilot loop + E-004 rewards (commit `32ce7b9`). React BeatPlayer over existing reducer + comps CSS: all beat types, review-nav already in reducer, localStorage + `PUT /api/progress` via `shouldPersist` (back-review writes nothing), Play tab label for beat-enabled landmarks, server-seeded resume + region stamp count, gesture-based `landmark_stamped` (resume never re-fires), stamp panel with 6 region pips + next-landmark offer + clean stop. Network-blocked Playwright pilot suite (8 specs) + desktop/mobile captures in `evidence/E-003/`. Hard boundaries held: no onboarding/OnboardingChat touch, no OpenRouter, no push, no new deps/routes. Gates at land: typecheck ✅ lint 0 errors/1 pre-existing warning ✅ 146 unit / 21 skipped ✅ build ✅ beats e2e 8/8 on :3100 ✅.


- 2026-07-19: Adversarially reviewed the standard SuperGrok/OAuth claim. Wrote `.frugal-fable/grok-sub-adversarial/findings.md` with first-party xAI documentation, GitHub issue review, July community reports, quota/economics analysis, and explicit retrieval limitations. Finding: PARTIALLY REFUTED. Standard-tier agent/OAuth access and X Premium+ eligibility are documented and have July working reports, but quota opacity and intermittent failures still justify an API key for predictable production/MoA capacity. `gh` API/comments retrieval was blocked by invalid local credentials and unavailable GitHub DNS; this is recorded in the evidence log.

- 2026-05-18: Created project-level `CLAUDE.md` and `AGENTS.md` with synchronized agent instructions for `/Users/thebeast/vibe-tutor`.
- 2026-05-18: Added this closeout ledger because the project directory does not yet have a git root or existing status/changelog file.
- 2026-05-18: Promoted the AIS-OS `/committee` skill to global Claude, Agents, and Codex skill homes.
- 2026-05-18: Added project skill routing for gstack stages and committee-driven decisions.
- 2026-05-18: Ran the Vibe Tutor product committee and saved the decision at `/Users/thebeast/AI-OS/research/committee/vibe-tutor-product/findings.md`.
- 2026-05-18: Built the first complete Vibe Tutor app as a local-first Vite + React + TypeScript 3Ms workflow tutor.
- 2026-05-18: Added deterministic answer feedback, typed lesson data, localStorage progress persistence, reset behavior, responsive Tutor Workspace UI, README, `.gitignore`, and progress-store tests.
- 2026-05-18: Verified with `npm run typecheck`, `npm run lint`, `npm run test`, `npm run build`, and gstack browser QA at `http://127.0.0.1:5173/`.

## Open To-Dos

- Decide whether to initialize a git repository and add a remote before any `/ship` workflow.
- Decide whether the next tutor pack should teach "build a small web app with AI" or stay focused on general 3Ms workflow operations.

## Nice-To-Haves

- Add a true AI-assisted feedback pass after deterministic feedback proves the lesson loop.
- Add export/share for the finished workflow block.

## Decisions / Notes

- `/Users/thebeast/vibe-tutor` is still a non-git project directory, but it now has a Vite + React + TypeScript local app.
- `CLAUDE.md` and `AGENTS.md` should remain synchronized to avoid agent-instruction drift.
- Use `/committee` for multi-frame decisions. Global skill paths: `/Users/thebeast/.claude/skills/committee/SKILL.md`, `/Users/thebeast/.agents/skills/committee/SKILL.md`, and `/Users/thebeast/.codex/skills/committee/SKILL.md`.
- Local run command: `cd /Users/thebeast/vibe-tutor && npm run dev -- --host 127.0.0.1`.

## Mission 2026-07-10-code-tutor-v1 execution

- 2026-07-19: engagement-v2 E-000 research + freeze. Two five-leaf research fleets (deleg_4b102524 + deleg_f8bfe6ac) both returned with 5/5 leaves dead on OpenRouter HTTP 429 (free model 8 req/min cap + five parallel lanes = guaranteed collision); zero durable bytes from either. Orchestrator self-synthesized from 11 anchor topics searched + primary sources extracted in-session (Dunlosky 2013, Pan & Carpenter 2023, Sweller/Chen/van Gog, Nunes & Drèze 2006, Kivetz et al. 2006, Ryan & Deci 2000, Kapur 2008, Kestin et al. 2025, FSRS benchmark, WCAG 2.2 SC 2.2.2/2.3.3, Gabarron 2025, Caselles-Pina 2023). Output: `docs/research/adhd-engagement-synthesis.md` (graded claims table, ADHD hypotheses flagged, linked bibliography R1-R16). Headline: adult-ADHD product evidence is thin; usability study is decisive. DESIGN_CONTRACT v1.3 + VALIDATION_CONTRACT + HANDOFF authored in `docs/missions/2026-07-19-code-tutor-engagement-v2/`; FROZEN under Lane B (original user directive = authorization). AGENTS.md/CLAUDE.md pointer switched to v2 for engagement work. Committed 12fe978. Foreign dirty file `.agents/skills/code-tutor-agent-lessons/SKILL.md` (parallel ISSUE-02x session) classified and left alone — excluded from all engagement commits. v1 HITL gates untouched.

- 2026-07-19: engagement-v2 E-001 backend complete. Shipped: beat zod grammar (9 types, 5-8 beats) + static server registry + pilot content fixture (`git/commits-as-checkpoints`, 8 beats, canonical-field copy only); registry-keyed `PUT /api/progress` (server registry decides the write path — forged/omitted `kind` cannot bypass beat validation; non-beat legacy untouched); atomic monotonic SQL merge (GREATEST/OR/first-stamp via two-sided `NULLIF(..., 'null'::jsonb)`); 5 new typed analytics events incl. `ClientAnalyticsEvent` Extract. Gates: typecheck pass; lint pass (1 pre-existing `_landmarkIds` warning); 122 passed / 21 skipped (beat unit + forge-bypass routing + merge semantics; route HTTP layer itself not directly mocked — decision logic unit-proven via `resolveProgressWrite`); build pass; manifest validates 1 sequence. Live SQL proof: guarded integration test committed, skipped (TEST_DATABASE_URL unset) — must run on disposable Postgres before E-003/E-004 treat progress as closed. Manifest timestamp churn restored. Contract amendments A1 (always-merge, no 409), A2 (pilot content as E-001 fixture), A3 (quiz = per-attempt event; analytics = 4 files). Committed b8d0213.

- 2026-07-19: engagement-v2 E-002 comps gate PASS. Three-state browser-rendered comps (predict / scenario-diff / stamp+next) in `designs/comps/engagement-v2/` (real HTML/CSS, exact product copy, real tokens — canonical over AI images per contract §7), six captures desktop+mobile via `scripts/render-comps.mjs` (Playwright, fonts asserted, objective assertions: no overflow incl. 8px shadow budget, ≥44px targets, single-line diff rows). Two vision inspection rounds; folklore rejected (Submit/Retry buttons, pip hit-targets, pixel+Inter "clash", jargon), real defects fixed once (pixel line-height 1.35, diff grid+0.78rem mobile, ghost secondary, stamp wrap padding). Judge record: `docs/missions/2026-07-19-code-tutor-engagement-v2/evidence/E-002/comp-judgement.md` — verdict PASS, BeatPlayer unblocked.

- 2026-07-18: ISSUE-029 unified all 13 reconciled analytics events behind typed, runtime-allowlisted client/server sink seams; removed profile identifiers and duplicate emissions; wired region selection; and added construction plus browser-dispatch coverage.

- 2026-07-18: ISSUE-028 implemented explicit immutable progress-share snapshots with payload-only privileged public reads, owner-scoped revocation, short-cache public summary and offline-safe OG image, accessible share/copy/revoke UI, stub analytics emission, and unit/e2e coverage. Completion is deterministic: only `state.completed === true` counts as completed; any recognized progress row starts its region.

- 2026-07-18: ISSUE-027 implemented the Stripe test-mode no-card trial, Checkout/cancel/reconciliation routes, transactional ordered/idempotent webhook processing, guide-only paywall, fixture-replay tests, and graceful unconfigured-billing e2e coverage. VAL-041 live test-card flow remains HITL-STRIPE-KEY deferred.

- 2026-07-18: ISSUE-026 drafted structured Terms, Privacy, and Cancellation & Refund policies with prominent legal-review warnings, shared readable rendering, a site-wide reusable legal footer, and VAL-043 Playwright coverage. HITL-LEGAL review remains required before launch.

- 2026-07-18: ISSUE-025 implemented landmark/profile-anchored guide chat, deterministic bounded Opus escalation with persisted decisions, Sonnet/Haiku gateway behavior, canonical offline fallback and signed-drill runtime coverage.

- 2026-07-18: ISSUE-024 implemented deterministic canonical overviews and quiz grading, a bounded access-gated lesson renderer with graceful fallback, profile-adaptive defaults, accessible URL-backed format switching, analytics stubs, and unit/e2e coverage.

- 2026-07-18: ISSUE-023 implemented a persisted, server-owned five-step onboarding profile state machine with deterministic access/gateway fallbacks, a non-blocking accessible map chat panel, and unit/e2e coverage for skip, unlock, malformed output, injection, denial, and token guards.

- 2026-07-17: ISSUE-014 bundle-budget slice deferred Pixi initialization until window load plus browser idle time, kept the DOM map fallback interactive immediately with a reduced-motion-aware canvas crossfade, and split client manifest access from server-side Zod validation.

- 2026-07-17: ISSUE-011 implemented the DOM-canonical eight-region map interaction reducer, lazy Pixi top-map presentation with deterministic CSS fallback, accessible controls and detail panel, keyboard/pointer camera controls, reduced-motion behavior, and unit/e2e coverage.

- 2026-07-17: ISSUE-010 implemented the taxonomy-locked schema-to-manifest content pipeline, 48 draft landmark modules, deterministic build validation and draft gate, committed-manifest runtime resolvers, and manifest-backed region UI/statistics.

- 2026-07-11: ISSUE-000 pre-flight. Dirty tree classified per clean-tree-before-work: 12 untracked files, ALL under docs/missions/2026-07-10-code-tutor-v1/** — mission packet authored by the orchestrator planning session 2026-07-10. No foreign/unknown-provenance files. Staged only packet paths + this ledger entry; no blanket add.
- 2026-07-11: DISCOVERED (baseline, pre-existing at 894a638): `npm run typecheck` fails — app/page.tsx imports @/components/MapExperience which does not exist in the repo or anywhere on disk (import of vibe-tutor scaffold lost/never had it). Routed to ISSUE-001 (scaffold normalization). ISSUE-000 is docs-only and does not alter gate status.
- 2026-07-11: ISSUE-000 addendum: package-lock.json (generated by orchestrator `npm install` during baseline gate — committed for reproducibility) and tsconfig.tsbuildinfo (tsc artifact — added `*.tsbuildinfo` to .gitignore). Both this session's provenance.
- 2026-07-10: ISSUE-001 normalized the package and repo guidance to code-tutor, added the project glossary and runtime content schemas, established the eight-region data invariants, removed the stub region state, restored the missing page component with an accessible placeholder list, and updated neutral metadata. Map implementation remains gated behind its later design artifact.
- 2026-07-11: ISSUE-001 executed by codex-worker (gpt-5.6-sol, workspace-write re-dispatch after read-only sandbox block). Orchestrator-authorized in-scope additions: minimal MapExperience placeholder + app/page.tsx fix (defect from ISSUE-000), eslint.config.mjs repair (FlatCompat incompatible with eslint-config-next 16 — replaced with native flat-config imports). next-env.d.ts/tsconfig.json diffs are `next build`-generated (orchestrator gate run) — committed as tool-generated. Full gate green: typecheck 0, lint 0, test 0 (3 pass), build 0. Fresh-context validator: all criteria PASS by inspection; its VAL-001 command runs failed only due to its own read-only sandbox EPERM — gate evidence taken from orchestrator run. Note for ISSUE-002: stray /Users/thebeast/package-lock.json triggers Next workspace-root warning; set turbopack.root in next.config.ts.
- 2026-07-11: ISSUE-002 (orchestrator in-session, credentialed). Vercel project code-tutor created + linked (scope desmond-landrys-projects); allowlisted in ~/.claude/vercel-project-map.json; deploy READY at https://code-tutor-pwio9q8xd-desmond-landrys-projects.vercel.app; env REST write/read-back/delete proven (MISSION_ENV_PROBE); turbopack.root pinned in next.config.ts; docs/DEPLOY.md added. PENDING USER ACTION: deployment returns 302 — team SSO deployment protection is on; permission classifier denied disabling it from this session. Disable in Vercel dashboard (code-tutor → Settings → Deployment Protection) or grant the API call.
- 2026-07-11: ISSUE-003 BLOCKED at Gemini verification call: IneligibleTierError (free Gemini Code Assist tier deprecated; oauth-personal unsupported; no GEMINI_API_KEY on machine). Required-tool-down rule: user must re-auth/migrate Gemini or provision an API key. ISSUE-004/009 remain dispatchable (independent dep branches).
- 2026-07-12: Blocker-clearing session (orchestrator in-session, credentialed). (1) Gemini vision backend restored: user provisioned GEMINI_API_KEY (stored chmod-600 at ~/.gemini-api-key, exported via ~/.zshenv); gemini CLI settings.json auth switched oauth-personal -> gemini-api-key; verified live: REST generateContent 200 text + vision, gemini CLI pong, mcp__gemini__ask-gemini pong. ISSUE-003 unblocked (status pending). (2) Vercel SSO deployment protection disabled via REST PATCH (user-authorized this session); deployment now 200 (was 302). ISSUE-002 HITL resolved. mission-state.json updated. Security note: key value was pasted into chat -> transcript ingests to gbrain; recommend rotation or /scrub-leaked-secret.
- 2026-07-15: ISSUE-003 UNBLOCKED — user provisioned new GEMINI_API_KEY in ~/.zshenv; generativelanguage API 200; ask-gemini verification call returned VISION-BACKEND-OK. NOTE: key value was pasted into chat by user — rotate or scrub after mission (scrub-leaked-secret).
- 2026-07-15: ISSUE-003 complete. 5 comps (Gemini imagegen REST; nano-banana MCP broken — mkdir /generated_imgs); Gemini 2.5 Pro vision judge; winner cozy-pixel (10/9/10/9); renderer Pixi.js recorded in designs/map-style.md. M0 exit still pending ISSUE-002 HITL (SSO protection → preview 302).
- 2026-07-15: Tree hygiene: gitignored .serena/ (Serena MCP auto-onboarding artifacts, this session) and .remember/ (memory-hook scratch; caused a stray eslint warning). Tool-generated provenance, both.
- 2026-07-16: ISSUE-002 HITL resolved — user approved disabling Vercel SSO deployment protection (AskUserQuestion); PATCH ssoProtection=null; site now public http 200. M0 exit complete.
- 2026-07-16: AMENDMENT A1 (user): Neon replaces Supabase for all data/auth issues. Recorded in docs/missions/2026-07-10-code-tutor-v1/AMENDMENTS.md; ISSUE-004 re-scoped to Neon + RLS + Neon-branch test stack.
- 2026-07-17: ISSUE-005 implemented app-issued HS256 anonymous sessions, server-side profile creation, RLS-scoped progress APIs, a root SessionProvider, and Playwright coverage for persistence and the absence of signup UI. The repo uses root `app/`, so active API routes were added there rather than the prompt's assumed `src/app/` path.
- 2026-07-17: ISSUE-005 test-tooling follow-up added Vitest aliases for the `server-only` test stub and the tsconfig-aligned `@/` source path, while keeping `e2e/**` outside the unit-test suite.
- 2026-07-17: ISSUE-006 implemented privileged custom email OTP challenges, upgrade-in-place and newest-wins collision merge logic, thin authenticated routes, a provider-free email transport seam, and the minimal save-progress modal. Added VAL-022 integration coverage (Neon-guarded) and unit coverage for normalization, hashing, and production transport failure.
- 2026-07-17: ISSUE-007 implemented the shared privileged access seam with versioned caps, atomic worst-case reservations, reconciliation and expiry, cross-surface identity accounting, anonymous IP/device throttles, global hard caps, UTC rollover behavior, and Neon-guarded VAL-035 concurrency/integration coverage.
- 2026-07-17: ISSUE-008 implemented the AI SDK v6 Gateway transport seam with configurable provider/model strings, a single 429 fallback, typed outage signaling, and a signed canary-only failure-drill harness with production forced-config rejection. Transport tests remain database- and network-free.
- 2026-07-17: ISSUE-009 locked all 48 taxonomy slots across eight regions, preserving the upstream Databases and Infra / Hosting taxonomies and adding six vibe-coder-first landmarks for each remaining region. Structural validation confirmed 8×6 slots, unique kebab-case IDs, explicit coherence checks, and the pending orchestrator sign-off placeholder.

- 2026-07-19: PM review pass (post-session, Claude). Verified engagement-v2 handoff claims against repo: commits 12fe978/b8d0213/035b22f present; typecheck clean; tests 135 passed / 21 skipped. Fixed the contradictory canAdvance assertion pair in src/__tests__/beatReducer.test.ts (kept `true` — reveal beat has 2 cards, revealCount=2; edit dispatched to glm-worker per frugal doctrine, verified by orchestrator). Updated HANDOFF.md with verified mid-flight state and the previously missing review-navigation spec (`back` re-gating + revealCount=0 on reveal re-entry). Root cause of the dead session recorded: Hermes "Kimi Crew" MoA aggregator was openrouter:moonshotai/kimi-k3 and the OpenRouter key exhausted its monthly credit cap (HTTP 402) mid-handoff; preset since repointed (K3 advisor via kimi-coding, gpt-5.6-sol aggregator).
- 2026-07-19: Repaired `designs/comps/engagement-v2/beat-comps.html` after direct user review exposed inert controls. Root cause: E-002 was rendered as three static visual states with clickable-looking native controls but no JavaScript or state machine. Added deterministic predict feedback, scenario wrong-answer retry, correct-answer completion, stamp transition, and explicit simulated Next/Map handoff states; preserved predict as unscored informational feedback. Added `scripts/test-beat-comps.mjs` and proved the full path on desktop and mobile. Updated the capture renderer to select states through the prototype controller and recreated all six captures. Verification: interaction test pass at 1280×800 and 390×844; comp capture assertions pass; typecheck pass; lint 0 errors/1 pre-existing warning; 135 tests pass/21 DB-guarded skipped; production build pass. Restored unrelated manifest churn produced by the build.
- 2026-07-19: Senior-dev cleanup pass (full codebase, frugal-fable: 3 Codex audit workers → orchestrator-vetted findings → 4 Codex fix slices, every diff reviewed + gated). P1 fixes: `verifySessionToken` no longer swallows a missing `AUTH_SECRET` as an anonymous session; Stripe webhook returns 500 (retryable) for non-signature failures via new `WebhookSignatureError`; `UpgradeAccountModal.submit`/`MapExperience.revokeShare` get try/catch/finally so network failures can't strand `busy=true`; beat schema rejects duplicate option ids; `validateBeatSequences` rejects sequences referencing missing canonical landmarks. P2: checkout `action` zod-validated (400 on invalid); lesson-progress write is now compare-and-swap; modal state resets on close; shared `landmark/formats.ts` and `map/regionAccents.ts` replace duplicated format/color registries (hex values verified identical); `content-client` reuses the schema-validated manifest loader; deleted dead `src/lib/regionStats.ts`, `src/content/draft.ts`, `allLandmarks`; `access.ts` uses the normalized day in all counter updates; `test:rls` fails fast without `TEST_DATABASE_URL`; eslint `ignoreRestSiblings` → lint 0 problems. Reducer: implemented HANDOFF review-navigation spec (reveal re-entry shows first card via shared `revealCountOnEntry`; below-frontier forward navigation never re-gates; frontier gate + write-nothing back-review covered by tests). Deferred deliberately: onboarding read/modify/write race (API-semantics change on frozen v1 surface — spawned as its own task). Gates: typecheck ✅ lint 0 problems ✅ 143 tests ✅ build ✅ beat-comps interaction smoke ✅.
- 2026-07-19: Authored KICKOFF-2026-07-19-beatplayer.md (rewritten engagement-v2 execution plan for a fresh Kimi Crew session: reducer review-nav fix → BeatPlayer → wiring/progress → network-blocked Playwright → E-004 rewards → E-005 transfer, stop before factory). Hard boundary recorded: parallel session owns the onboarding-route race fix (app/api/onboarding/route.ts, OnboardingChat.tsx) — engagement session must fast-forward first and never touch those files. HANDOFF.md pointer added.
- 2026-07-19: Onboarding read/modify/write race fixed (spawned task from the senior-dev cleanup pass). `app/api/onboarding/route.ts` now does the model parse (`parseAnswer`) against an unlocked read, then re-reads `onboarding_state` under `SELECT ... FOR UPDATE` inside a single `withUserTransaction` and applies answer + state save atomically; a submission whose step advanced concurrently gets 409 with no write, so profile columns and `onboarding_state` can no longer disagree. `applyAnswer` gained an optional client param to run inside the transaction; `OnboardingChat` re-syncs via `start` on 409. Behavior otherwise unchanged (same 400s, same response shape, `profile_skipped`/`profile_built` events now emitted from locked state after commit). Added `src/__tests__/onboardingRoute.test.ts` route-level regression (stale concurrent answer → 409, winner preserved). Worktree note: build previously failed here because the worktree had a stub `node_modules` (no `next`) — `npm install` fixed it; failure reproduced at HEAD, unrelated to this change. Gates: typecheck ✅ lint 0 errors/1 pre-existing exhaustive-deps warning ✅ 146 tests ✅ build ✅.
- 2026-07-19: Kickoff packet re-verified after b47d69a/5f4d786 landed mid-review (comps prototype + review-nav implemented): work item 1 dropped, baseline updated to 146 passed / 21 skipped. Fixed vitest sweeping sibling Claude worktrees (.claude/worktrees/ from the parallel onboarding session) — added '.claude/**' to test.exclude in vitest.config.ts (edit via glm-worker). Root suite verified clean post-fix.
- 2026-07-19: /constance-install run: hooks wired, operating contract fenced into CLAUDE.md, 38 project constants declared from CLAUDE.md/AGENTS.md + v2 mission docs (120 claims audited via glm-worker sweeps; snapshots and self-referential claims filtered). Report: .constance-bootstrap/INSTALL-REPORT.md. Trigger: sourcing-bias failure in SuperGrok research (adversarial review PARTIALLY REFUTED the original claim).

- 2026-07-20: Launch-mission planning session (orchestrator + owner, grilled via AskUserQuestion). Verified engagement-v2 close state live (typecheck clean; 146 passed / 21 skipped — matches HANDOFF). Owner decisions (Amendment A4, recorded in docs/missions/2026-07-20-vibe-code-quest-launch/KICKOFF.md + Constance user-decision constants): factory before usability study (study post-launch); product goes free + open-source (MIT, public GitHub) as a Trueline/Constance marketing asset; name "Vibe Code Quest by Trueline"; leaderboard authorized (overrides v2 forbidden pattern #5 — positive-only copy both directions, opt-in handle, XP-ranked, weekly+all-time); ship XP + collectibles + map glow (streaks stay excluded); paywall gating removed with billing seam dormant; production deploy authorized; legal pages ship adapted with draft warnings kept until lawyer review; hosted guide default → Kimi K2 via gateway seam with existing caps; hosted Neon stays; no-DB self-host mode required; disposable Neon branch authorized for live SQL proof. v2 DESIGN_CONTRACT untouched (frozen); v2 HANDOFF gained a superseded-pointer block. Handoff prompt authored for Hermes Kimi-Crew AFK execution.
