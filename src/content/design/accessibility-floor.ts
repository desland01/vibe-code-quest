import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("accessibility-floor", "Accessibility floor", "Semantic controls, keyboard access, focus, contrast, and reduced motion form a minimum interaction contract.");
