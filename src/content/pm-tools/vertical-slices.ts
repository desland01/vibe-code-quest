import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("vertical-slices", "Scope as vertical slices", "A vertical slice delivers the smallest testable user outcome across every necessary layer.");
