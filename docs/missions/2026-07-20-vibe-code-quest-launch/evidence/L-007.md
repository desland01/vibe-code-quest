# L-007 — "Vibe Code Quest by Truline" rebrand

**Date:** 2026-07-28
**Repo:** `/Users/thebeast/code-tutor` · **Branch:** `main` · **Parent commit:** `96f266f`
**Authorization:** KICKOFF A4.3 (amended 2026-07-21 to the **Truline** spelling) +
EXECUTION_PLAN §6 + GRILL-DECISIONS **G-4** (rebrand user-visible surfaces only).

---

## 1. Naming rule applied

- Full public product name string: **`Vibe Code Quest by Truline`**
- Standalone short form where the full string would crowd a heading: **`Vibe Code Quest`**
- **Truline**, never "Trueline". The word `Truline` in the footer byline links
  `https://truline.io`.
- Tagline unchanged: **"A Map for Post-AI Builders"**.

## 2. Surfaces changed

| File | Change |
|---|---|
| `app/layout.tsx` | `title` → `Vibe Code Quest by Truline — A Map for Post-AI Builders`. `description` is now a real description instead of a duplicate of the title: "A free learning map for people building software with AI agents. 48 landmarks across 8 regions." |
| `src/components/MapExperience.tsx` | `<h1>` → `Vibe Code Quest`. Tagline paragraph untouched. |
| `src/components/SiteFooter.tsx` | **New byline block** under the existing legal nav: `Vibe Code Quest by Truline` (`data-testid="site-byline"`, `Truline` → `https://truline.io`, `rel="noreferrer"`) and `Governed by Constance` (`data-testid="site-constance"`). Existing nav and its three legal links untouched. Inline styles, matching the file's existing approach — no new CSS file, no new dependency. |
| `app/s/[token]/page.tsx` | Share metadata title/description and the `region-kicker` line rebranded. |
| `app/s/[token]/opengraph-image.tsx` | `alt` → "Vibe Code Quest progress snapshot"; OG kicker → `VIBE CODE QUEST · PROGRESS MAP`. Layout, colours and fallback subtitle logic unchanged. |
| `src/components/legal/LegalPage.tsx` | Not-legal-advice line now names Vibe Code Quest. |
| `src/content/legal/terms.ts` · `privacy.ts` · `refund.ts` | Every prose occurrence rebranded. Section counts, headings structure, `lastUpdated`, operator identity, jurisdiction and contact address are all unchanged from L-006. |
| `e2e/legal.spec.ts` | New `public brand is shown on the map and footer` test, plus two extra assertions on every legal page. |

`terms.ts` §1 was rewritten rather than substituted, so the product name and the operator name
do not collide: *"These Terms of Service govern your use of Vibe Code Quest by Truline, "A Map
for Post-AI Builders" (the "service"), operated by Truline, a trade name of Desmond Landry…"*.
`refund.ts` §1 heading and the §2 "misuse of the … name" sentence were handled the same way.

React-key safety: the renderer uses each paragraph string as its key, so string uniqueness was
re-verified after the edits — terms 28 strings, privacy 29, refund 7, **0 duplicates**.

## 3. Deliberately NOT renamed

| Kept | Why |
|---|---|
| `src/lib/auth/session.ts:16-17` — `TOKEN_AUDIENCE` / `TOKEN_ISSUER` = `'code-tutor'` | Grill defect #2. Both values are baked into every JWT already issued; renaming them invalidates every existing user session. |
| `src/components/map/MapCanvas.tsx:124` — `seedFrom('code-tutor-sea')` | A deterministic PRNG seed for the sea dither pattern, not user-visible text. Changing it changes the rendered map. |
| `package.json` `"name": "code-tutor"` | npm package name, not a public surface. |
| `docs/**`, `WORK_LEDGER.md`, mission directory names | Historical record (R018). |
| `.vercel/project.json` | Links by `projectId`; the local file needs no edit. |

These three source lines are exactly what the post-change scan is allowed to return, and it
returns nothing else.

## 4. Verification (2026-07-28)

### 4.1 Four gates

| Gate | Result |
|---|---|
| `npm run typecheck` | ✅ clean |
| `npm run lint` | ✅ 0 errors, 1 warning (pre-existing `OnboardingChat.tsx` exhaustive-deps) |
| `npm run test` | ✅ 180 passed / 39 skipped |
| `npm run build` | ✅ 48 sequences; all routes emitted |

### 4.2 Playwright — `http://localhost:3100` only, `--workers=1`

**Full suite: 51 passed / 51** (50 prior + the new brand test).

The new `e2e/legal.spec.ts` coverage asserts, on `/`: `<h1>` is `Vibe Code Quest`; the footer
byline reads exactly `Vibe Code Quest by Truline`; its `Truline` link href is
`https://truline.io`; the Constance line reads `Governed by Constance`; and the document title
matches `/Vibe Code Quest by Truline/`. Every legal page additionally asserts its body text
contains no `code-tutor` and no `Trueline`.

### 4.3 Rendered brand capture — desktop 1280×800 + mobile 390×844

`scripts/l007-brand-capture.mjs` — **exit 0, all 22 assertions PASS**. Per viewport it checks
the document title, the `<h1>`, the byline text, the byline href, the Constance line, absence of
`code-tutor`, absence of `Trueline`, zero horizontal overflow, and then on `/legal/terms`
absence of the legacy brand, presence of the new brand, and absence of `[BRACKET]` placeholders.

Captures in [`evidence/L-007/`](./L-007/):
`map-header-desktop.png` · `map-header-mobile.png` · `footer-desktop.png` · `footer-mobile.png` ·
`legal-terms-desktop.png` · `legal-terms-mobile.png`

Rendered inspection (orchestrator, both viewports): the mobile map header shows
"Vibe Code Quest" in the pixel display face on two comfortable lines with the tagline, XP chip
and Quest board intact; the footer byline sits centred and small under the legal nav without
competing with it. In the desktop footer capture the onboarding panel overlays the right-hand
legal link — that is the pre-existing fixed-position onboarding dialog sitting above the footer,
not a rebrand layout defect, and the mechanical overflow check measured **0 px**.

### 4.4 Mechanical scans

```
grep -rn "code-tutor" --include='*.ts' --include='*.tsx' app src
    → exactly 3 lines, all protected:
      src/components/map/MapCanvas.tsx:124   (PRNG seed)
      src/lib/auth/session.ts:16             (TOKEN_AUDIENCE)
      src/lib/auth/session.ts:17             (TOKEN_ISSUER)

grep -rni "trueline" app src e2e docs
    → 3 lines, none of them a product surface:
      e2e/legal.spec.ts:21                   (the assertion that forbids it)
      KICKOFF.md:42 / HANDOFF.md:21          (the rule stating the correct spelling)
```

---

## 5. Deferred owner action — Vercel project rename

G-4 also says: *"Rename the Vercel project to `vibe-code-quest`"*. **This was attempted and
blocked**, and is therefore **not done**.

```
vercel project rename code-tutor vibe-code-quest
→ denied by the local permission layer: renaming a live, production-linked
  Vercel project is a shared-infrastructure mutation that requires an explicit
  owner instruction naming the rename, which a mission document cannot supply.
```

Nothing else in this slice depends on it:

- `.vercel/project.json` links by `projectId` (`prj_WClQa03dfcb9UG1AE9aONshzxZzk`), not by name,
  so the local link keeps working and L-010 can deploy either way.
- The only visible consequence of leaving it is that the default deployment hostname stays
  `code-tutor-*.vercel.app` instead of `vibe-code-quest-*.vercel.app`.
- Nothing has launched yet, so the rename remains safe to do at any point before or after
  L-010.

**Owner action:** run the command above yourself, or approve it in a session, and L-010 will
pick up the new hostname. Vercel CLI is authenticated as `desmond-5183`.

---

## 6. Files in this commit

```
app/layout.tsx
app/s/[token]/opengraph-image.tsx
app/s/[token]/page.tsx
e2e/legal.spec.ts
src/components/MapExperience.tsx
src/components/SiteFooter.tsx
src/components/legal/LegalPage.tsx
src/content/legal/privacy.ts
src/content/legal/refund.ts
src/content/legal/terms.ts
docs/missions/2026-07-20-vibe-code-quest-launch/evidence/L-007.md
docs/missions/2026-07-20-vibe-code-quest-launch/evidence/L-007/*.png
docs/missions/2026-07-20-vibe-code-quest-launch/scripts/l007-brand-capture.mjs
WORK_LEDGER.md
```

Left uncommitted on purpose: `CLAUDE.md`, `constants.md`, `constants.md.reground-log.jsonl`,
`.claude/*`, `.agents/*`, `next-env.d.ts`, `public/content-manifest.v1.json`, and the
prior-slice evidence PNGs / `axe-report.json` that the e2e run re-renders in place.

## 7. Carried forward

`README.md` does not exist yet, so there was no README to rebrand here — EXECUTION_PLAN §6
lists it, but grill defect #3 established that both `README.md` and `LICENSE` must be
**created** in L-008. The brand strings go in when the file is written.
