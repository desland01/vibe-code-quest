You are a bounded worker agent executing exactly ONE issue in the repo at /Users/thebeast/code-tutor. Make no commits. No npm install (stripe@22 already installed). Build fails on fonts in your sandbox — run typecheck/lint/test only; orchestrator runs build + e2e. NEVER use or require live Stripe keys — everything here is TEST-MODE + fixture-replay.

# ISSUE-027 — Stripe trial + webhooks + paywall (REQ-013; VAL-040, VAL-041, VAL-042) — TEST MODE ONLY

Bound: db/migrations/0008_billing.sql (new), src/server/billing.ts (new — entitlement state machine + webhook processor + checkout builder), src/server/stripe.ts (new — thin Stripe client factory, injectable), app/api/billing/checkout/route.ts + app/api/billing/cancel/route.ts + app/api/stripe/webhook/route.ts (new), src/components/Paywall.tsx (new) wired into GuideChat/LandmarkView so the guide is gated, src/__tests__/billing.test.ts (new — fixture-replay), e2e/paywall.spec.ts (new), .env.example (names only: STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PUBLISHABLE_KEY, STRIPE_PRICE_ID). Do NOT touch content/map/onboarding logic.

Context (zero chat context assumed):
- REQ-013: no-card 14-day trial → subscription. Lifecycle: verified email (profiles.email set via ISSUE-006 OTP) → trial_started: server creates a Stripe customer + a subscription with trial_period_days=14 and payment_behavior requiring NO card (default_incomplete is NOT used; use a trial-without-payment-method config) → trial ends → paywall (no charge possible, no card) → explicit subscribe → Checkout (card) → active. ONE trial per verified email AND per Stripe customer (second attempt rejected). Entitlement state machine in the entitlements table: fields {stripe_customer_id, stripe_subscription_id, status, trial_end, current_period_end, cancel_at_period_end, last_event_id, last_event_created}. Idempotent transactional webhook handlers; out-of-order precedence by event.created; post-Checkout server-side reconciliation fetch; unique processed-event ids (processed_webhook_events table exists from 0001: event_id PK). Checkout sessions created server-side from the verified JWT only; profile id in Stripe metadata + a unique server-owned mapping. Price config-driven (STRIPE_PRICE_ID; test-mode placeholder $9/mo). Emit trial_started/subscribe_clicked/paywall_shown events. TEST MODE ONLY; live keys HITL.
- Existing: entitlements table (0001) has profile_id PK, tier, trial_starts_at, trial_ends_at, status, updated_at — EXTEND it in 0008 with the stripe/period fields above (ADD COLUMN IF NOT EXISTS; RLS: entitlements is SELECT-own / writes privileged-only per 0002 — webhook writes use the PRIVILEGED pool, not app_user). processed_webhook_events (0001) for idempotency. src/lib/db.ts pool (privileged) + queryAsUser. Access seam src/server/access.ts resolves tier from entitlements (checkAccess surface 'guide'); paywall = when trial expired and status not active, guide surface is denied but canonical content stays readable. Session ct_session.
- src/server/stripe.ts: `getStripe()` returns a Stripe client from STRIPE_SECRET_KEY, or throws a clear "Stripe not configured" error if unset. ALL billing functions accept an INJECTED stripe client + injected db so fixture tests never touch network or need a key. NO live key is required to build or test this issue.

Tasks:
1. Migration 0008_billing.sql: ALTER entitlements ADD COLUMN IF NOT EXISTS stripe_customer_id text, stripe_subscription_id text, current_period_end timestamptz, cancel_at_period_end boolean DEFAULT false, last_event_id text, last_event_created bigint; a UNIQUE server-owned mapping (unique index on stripe_customer_id where not null, and on stripe_subscription_id where not null); keep RLS/immutability from 0002 intact. Idempotent.
2. src/server/billing.ts:
   - startTrial({ userId, email, deps }): reject if the email or an existing customer already used a trial (one-per-email + one-per-customer); else create customer + trial subscription (trial_period_days=14, no payment method) via injected stripe; persist entitlement {status:'trialing', trial_end, stripe_customer_id, stripe_subscription_id} on the privileged pool; emit trial_started. Deterministic + injectable.
   - createCheckout({ userId, deps }): server-side Stripe Checkout session (mode subscription, the configured price, customer = the user's mapped customer, metadata.profile_id = userId); returns the URL. Requires the mapped customer.
   - processWebhookEvent(rawBody, signature, deps): verify signature (injected stripe.webhooks.constructEvent); idempotency: if event.id already in processed_webhook_events → no-op; ordering: ignore an event whose event.created < stored last_event_created for that subscription (precedence); handle customer.subscription.created/updated/deleted + checkout.session.completed + invoice.payment_failed → update entitlement status/current_period_end/cancel_at_period_end transactionally; record event.id in processed_webhook_events in the SAME transaction (retry-idempotent). Unknown customer/subscription → safe no-op. deleted subscription → status canceled.
   - reconcileAfterCheckout({ userId, deps }): fetch the subscription from Stripe and update entitlement (covers Checkout-return-before-webhook).
3. Routes: /api/billing/checkout (401-gated, requires verified email, calls startTrial-then/or createCheckout as appropriate; emit subscribe_clicked), /api/billing/cancel (set cancel_at_period_end via Stripe + persist), /api/stripe/webhook (raw body, signature header, calls processWebhookEvent; returns 200 quickly; never trusts client). If STRIPE not configured, checkout/cancel return a clear 503 "billing not configured" (so dev without keys degrades gracefully) — the webhook still verifies signatures when configured.
4. Paywall.tsx: shown when guide access is denied due to expired trial / inactive status; message + "Start free trial" (if no email → prompt OTP upgrade first) / "Subscribe" (test mode) buttons; emits paywall_shown. CANONICAL content (overview/quiz/lesson-fallback) remains fully readable — only the live guide is gated.
5. src/__tests__/billing.test.ts (fixture-replay, NO network, NO key — inject fake stripe + fake db):
   - trial: startTrial creates trialing entitlement; second trial for same email rejected; same customer rejected.
   - webhook signature verification (injected constructEvent throws on bad sig → handler rejects/no-ops safely).
   - duplicate delivery: same event.id twice → second is a no-op (processed_webhook_events).
   - out-of-order: an older event.created after a newer one → ignored (precedence).
   - handler retry idempotency: replaying the same event twice yields the same entitlement.
   - unknown customer → safe no-op; subscription.deleted → status canceled; checkout.session.completed → active + current_period_end set.
   - reconciliation: reconcileAfterCheckout updates entitlement when the webhook hasn't arrived.
   - price read from STRIPE_PRICE_ID config.
6. e2e/paywall.spec.ts: with billing NOT configured in dev, assert canonical content (overview) is fully readable on a landmark AND the guide surface shows the Paywall/appropriate gating without crashing (graceful 503 path). (The live subscribe-with-test-card flow is deferred — see below.) Keep all existing e2e green.

Validation: VAL-040 (fixture-replay integration tests — all above), VAL-042 (no live keys anywhere — .env.example names only). VAL-041's live subscribe-with-test-card Playwright flow is DEFERRED to when a Stripe TEST key is provided (orchestrator will note HITL-STRIPE-KEY); build the paywall + gating so that flow is ready. VAL-001/003 gate.

Stop conditions: existing seams/migrations don't match, or a command fails twice → STOP. Do NOT require a live Stripe key to make tests pass.

Print EXACTLY this structured handoff: Completed work / Unresolved work / Files touched / Commands run (with exit codes) / Issues surprises discovered / Next Context Slice.
