import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("reading-generated-code", "Reading code you did not write", "Trace inputs, outputs, side effects, and failures so generated code can be verified before shipping.");
