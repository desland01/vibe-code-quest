import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("typography-and-hierarchy", "Typography and hierarchy", "Type scale, weight, line length, and contrast communicate importance and grouping.");
