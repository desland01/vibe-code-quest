import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("graph", "Graph", "Graph storage centers relationships when connections between entities are the main data.");
