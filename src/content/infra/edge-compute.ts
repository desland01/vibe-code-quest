import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("edge-compute", "Edge compute", "Edge compute runs bounded logic near users when latency and geographic distribution matter.");
