import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("backlog-vs-now", "Backlog vs now", "A backlog preserves possibilities while a small active queue keeps current execution focused.");
