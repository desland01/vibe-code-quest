import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("dependency-supply-chain", "Dependency and supply-chain risk", "Every dependency and external build step adds code, provenance, and maintenance risk.");
