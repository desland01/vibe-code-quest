import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("html-css", "HTML and CSS", "HTML gives interfaces meaning and CSS controls their presentation, layout, and responsiveness.");
