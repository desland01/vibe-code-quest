import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("secrets-and-environment", "Secrets and environment", "Secrets are credentials that belong in protected server-side stores, never source, browsers, prompts, or logs.");
