import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("ai-evals", "Evals", "Evals are repeatable checks for whether AI behavior remains useful, correct, and safe enough.");
