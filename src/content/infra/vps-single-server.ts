import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("vps-single-server", "VPS / single server", "A single virtual server offers a straightforward always-on runtime with explicit operational responsibility.");
