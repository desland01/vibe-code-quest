import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("tool-use", "Tool use", "Tool use lets a model request narrowly defined actions through validated schemas.");
