import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("python", "Python", "Python is a direct, library-rich language suited to scripts, data work, and many AI services.");
