import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("model-call-vs-agent", "Model call vs agent", "A model call produces one bounded response, while an agent can choose and execute multiple steps toward a goal.");
