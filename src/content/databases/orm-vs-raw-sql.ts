import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("orm-vs-raw-sql", "ORM vs raw SQL", "ORMs provide generated query abstractions while raw SQL gives direct control over database operations.");
