import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("commits-as-checkpoints", "Commits as checkpoints", "A commit is a named, inspectable snapshot of one coherent change.");
