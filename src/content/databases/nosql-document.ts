import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("nosql-document", "NoSQL document", "Document storage keeps related data together when its shape is still changing or does not fit relational tables cleanly.");
