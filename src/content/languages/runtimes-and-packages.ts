import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("runtimes-and-packages", "Runtimes and packages", "Code depends on the environment that executes it and the package ecosystem it borrows from.");
