import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("merge-conflicts", "Merge conflicts", "A merge conflict means two histories changed the same area and require an intentional combined result.");
