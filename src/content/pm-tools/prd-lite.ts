import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("prd-lite", "PRD-lite", "A lightweight product brief aligns the user, problem, promise, constraints, and success signal.");
