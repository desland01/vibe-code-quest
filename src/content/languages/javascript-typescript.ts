import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("javascript-typescript", "JavaScript and TypeScript", "JavaScript runs widely on the web, while TypeScript adds contracts that expose mismatches before runtime.");
