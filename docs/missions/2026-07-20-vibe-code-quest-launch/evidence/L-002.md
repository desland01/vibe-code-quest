# L-002 — Beat factory (48 landmarks)

**UTC close:** 2026-07-21
**Result:** PASS
**Base:** L-001 `3ab22f5`

## Registry

- Total sequences: **48**
- Hand-authored overrides (unchanged): `git/commits-as-checkpoints`, `security/trust-boundaries`
- Factory-derived: **46** via `src/content/beats/derive.ts`
- Grammar (all sequences): `hook → predict → reveal → scenario → gotcha → default → check → recap` (8 beats)
- Build validates all 48 keys at `npm run build`

## Copy loyalty / provenance

- Claim-bearing strings come only from **the same landmark’s** canonical fields (`hook`, `title`, `definition`, `example`, `when_to_use`, `tradeoffs`, `gotchas`, `vibe_coder_default`, `quiz`) plus a fixed allowlisted framing set in `FACTORY_FRAMING`.
- No sibling-landmark text.
- No LLM-invented facts.
- Unit tests lock:
  - exact field mapping for derived beats
  - exact-composite provenance allowlist (no startsWith loopholes)
  - deterministic re-derive equality
  - correct-option slot rotation across the factory set (not always A)
  - **anti-spoiler:** quiz question / answer / explanation absent from beats 0–5 (check owns the quiz)
- Collision audit: 0 of 46 derived landmarks have quiz.answer equal to factory correct labels for predict/scenario/gotcha source fields.

## Feedback honesty (source-aware, no invented facts)

- Every choice option has deterministic per-option feedback.
- **Correct options:** fixed positive lead from `FACTORY_FRAMING`
  (`predictCorrectLead` / `scenarioCorrectLead` / `gotchaCorrectLead`).
- **Wrong options:** fixed source-class frame + the option’s exact canonical label
  (e.g. `predictWrongConPrefix + label`, `gotchaWrongProPrefix + label`).
  Source classes are the same-landmark field the label came from
  (`pro` / `con` / `gotcha` / `when_to_use` / `default`).
- Distinct wrong options therefore get distinct feedback without inventing facts.
- Unit locks: wrong feedback ∈ `allowedWrongFeedbacks(landmark)`; feedback includes
  the exact option label; per-beat uniqueness of wrong-feedback strings.

## Fallout fixes caused by 48-beat enablement

1. **Invalid `?format=` fallback** (`app/map/[region]/[landmark]/page.tsx`):
   missing format → beat-aware default (Play); **explicit invalid format → overview** (trust surface), not Play. Restores `e2e/map-sub` contract.
2. **Analytics heading strict-mode** (`e2e/analytics.spec.ts`):
   BeatPlayer also renders an h3 title; pin page header via `#landmark-title`.
3. **Landmark formats IA** (`e2e/landmark-formats.spec.ts`):
   every landmark is beat-enabled → tab label **Play** (not Lesson); BeatPlayer mounts. Intentional A4/L-002 IA, not a timeout-bump of the parallel-load flake.

## Spot-checks (one landmark per region)

| Region | Landmark | Path |
|---|---|---|
| languages | python | stamp ✅ |
| databases | sql | stamp ✅ + mobile stamp ✅ |
| infra | serverless-functions | stamp ✅ |
| ai-types | tool-use | stamp ✅ |
| pm-tools | issues-as-specs | stamp ✅ |
| git | branches-as-isolation | stamp ✅ |
| security | secrets-and-environment | stamp ✅ |
| design | design-tokens | stamp ✅ |

File: `e2e/factory-spotcheck.spec.ts` (registry-driven correct options; no hard-coded A/B/C).

## Visual / layout proof

**Vision-analyze provider was credit-blocked (OpenRouter 402).** No alternate vision path used this run. Substitute evidence only:

1. Rendered screenshots under `evidence/L-002/` (available for owner eyeballs; **not** claimed as subjective visual PASS).
2. Mechanical browser geometry assertions at **scenario** and **stamp** surfaces (PASS):
   - no page horizontal overflow (`scrollWidth ≤ viewportWidth`)
   - no option/control outside viewport
   - no internal `scrollWidth > clientWidth` on choices/advance/stamp/next/back

### Screenshot inventory (sips)

| File | Size |
|---|---|
| databases-sql-hook-desktop.png | 1280×720 |
| databases-sql-scenario-desktop.png | 1280×720 |
| databases-sql-scenario-mobile.png | 390×844 |
| databases-sql-stamp-desktop.png | 1280×720 |
| databases-sql-stamp-mobile.png | 390×844 |
| git-branches-as-isolation-hook-desktop.png | 1280×720 |
| git-branches-as-isolation-scenario-desktop.png | 1280×720 |
| git-branches-as-isolation-stamp-desktop.png | 1280×720 |

Mechanical overflow assertions: **PASS** on all 8 region spot-checks + mobile sql path.

## Gates

| Gate | Result |
|---|---|
| `npm run typecheck` | ✅ 0 |
| `npm run lint` | ✅ 0 errors / 1 pre-existing OnboardingChat warning |
| `npm run test` | ✅ 149 passed / 22 skipped (includes JWT payload-tamper stabilization in `session.test.ts`) |
| `npm run build` | ✅ 48 beat sequences validated |
| Playwright targeted (factory + beats + landmark-formats + analytics + map-sub) | ✅ |
| Playwright **full suite** `PLAYWRIGHT_BASE_URL=http://localhost:3100` workers=1 | ✅ **44/44** |

## Hydration lesson (e2e)

Playwright against `http://127.0.0.1:3100` while Next serves `localhost` blocks HMR/hydration (`allowedDevOrigins`), leaving Begin inert. **Always pin e2e to `http://localhost:3100`.** Not a product bug.

## Boundaries held

- Frozen `DESIGN_CONTRACT.md` not edited (R001)
- Canonical landmark files / VOICE.md not edited
- Constance self-writes / `.claude/*` / `.agents/*` not committed
- Manifest timestamp churn restored uncommitted
- Historical engagement-v2 evidence PNGs restored after suite capture side-effects

## Follow-ons (not L-002)

- L-003 XP must be **server-derived** from existing progress facts (`furthestBeatIndex` milestones, `checked`, `completed`) — do not extend the frozen 6-key beat progress shape without an amendment.
- Vision provider still credit-blocked; L-005/L-010 may need owner eyeballs or an alternate local visual surface if vision remains down.
