import { createDraftLandmark } from '../draft.ts';

export const landmark = createDraftLandmark("layout-and-spacing-rhythm", "Layout and spacing rhythm", "Layout rules create consistent alignment, density, and responsive structure across screens.");
